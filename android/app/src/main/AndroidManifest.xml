<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.moni_prayer">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Android 11+ (API 30+) এ app-specific ফোল্ডারের বাইরে (পাবলিক
         Download/... এর মতো জায়গায়) ফাইল লেখার জন্য এই পারমিশন লাগে।
         এটা দিয়েই কোরআনের mp3 ফাইলগুলো এমন একটা পাবলিক পাথে রাখা হচ্ছে
         যেটা অ্যাপ আনইনস্টল করলেও মুছে যায় না — app-specific
         /Android/data/<package>/... পাথ আনইনস্টলে স্বয়ংক্রিয়ভাবে মুছে
         যায় (এটা Android নিজেই করে, কোনো অ্যাপ কোড দিয়ে আটকানো যায়
         না), কিন্তু পাবলিক পাথ মুছে যায় না।
         নোট: Google Play policy অনুযায়ী এই পারমিশনের জন্য পাবলিশ করার
         সময় বিশেষ justification লাগে — এই অ্যাপ এখন শুধু ব্যক্তিগত
         ব্যবহারের (সাইডলোড) জন্য, তাই এটা ব্যবহার করা হচ্ছে। Play
         Store-এ পাবলিশ করার সময় এটা MediaStore-ভিত্তিক পদ্ধতিতে পরিবর্তন
         করা উচিত। -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ফোন রিস্টার্ট হলে flutter_local_notifications-এর শিডিউল করা
         exact alarm গুলো (নামাজের সময়/নিষিদ্ধ সময়/সেহরি ইত্যাদি
         notification) মুছে যায় — এই পারমিশন + প্যাকেজের নিজস্ব
         boot receiver দিয়ে রিস্টার্টের পর সেগুলো আবার সক্রিয় হয়। -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="MONI PRAYER"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <meta-data
                android:name="com.ryanheise.audioservice.componentName"
                android:value="com.example.moni_prayer.MainActivity"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ══ audio_service: background Quran playback (screen off / app minimized) ══ -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- ══ Prayer Clock Widget ══ -->
        <receiver android:name=".PrayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>

        <!-- ══ flutter_local_notifications: ফোন রিস্টার্ট হলে শিডিউল করা
             notification গুলো (নামাজের ওয়াক্ত/নিষিদ্ধ সময়/সেহরি ইত্যাদি)
             আবার সক্রিয় করে, এবং notification-এর একশন বাটন হ্যান্ডল করে ══ -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
    </application>
</manifest>
